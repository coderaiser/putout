{
    "match": {
        "**/packages/plugin-*": {
            "putout": "on"
        }
    },
    "rules": {
        "remove-useless-spread/object": ["on", {
            "exclude": "module.exports.rules = __"
        }]
    }
}
