const x = 1;
const y = 2;
const z = 3;

const msg = `${x}`;

console.log(`${z}${y + 1}`);

const f = (a) => `${1 + 2} ${a}`;

function assign(name, password) {
    module.exports = `${name}${password}`;
}

