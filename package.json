{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "FORCE_COLOR=1 lerna run test",
    "coverage": "FORCE_COLOR=1 lerna run coverage",
    "report": "lerna run report",
    "lint": "lerna run lint",
    "fix:lint": "lerna run fix:lint",
    "bootstrap": "lerna bootstrap"
  },
  "husky": {
    "hooks": {
      "pre-commit": "putout -m"
    }
  },
  "devDependencies": {
    "husky": "^3.0.5",
    "lerna": "^3.8.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {}
}
