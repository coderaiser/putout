{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "FORCE_COLOR=3 lerna run test",
    "coverage": "FORCE_COLOR=3 lerna run coverage",
    "report": "lerna run --no-bail report",
    "lint": "FORCE_COLOR=3 lerna run --no-bail lint",
    "fix:lint": "lerna run --no-bail fix:lint",
    "bootstrap": "lerna bootstrap"
  },
  "husky": {
    "hooks": {
      "pre-commit": "putout -a"
    }
  },
  "devDependencies": {
    "husky": "^3.0.5",
    "lerna": "^3.8.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {}
}
