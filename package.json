{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "coverage": "lerna run coverage",
    "report": "lerna run report",
    "lint": "lerna run lint",
    "bootstrap": "lerna bootstrap --concurrency=1"
  },
  "devDependencies": {
    "lerna": "^3.8.5"
  }
}
