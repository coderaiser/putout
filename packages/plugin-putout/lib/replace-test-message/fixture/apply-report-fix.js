test('plugin-putout: convert-traverse-to-include: report', (t) => {
    t.report('traverse', 'x');
    t.end();
});

test('plugin-putout: convert-traverse-to-include: report', (t) => {
    t.report('traverse', 'x');
    t.end();
});
