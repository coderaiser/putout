const {
    isNumericLiteral,
    isNumberLiteral,
} = require('putout').types;
const {
    NumberLiteral,
    NumericLiteral,
} = require('putout').types;

isNumericLiteral(a);
isNumericLiteral(a);
NumericLiteral(a);
