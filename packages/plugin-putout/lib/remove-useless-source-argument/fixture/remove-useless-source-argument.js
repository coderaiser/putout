transform(ast, source, options);
findPlaces(ast, source, options);

tryCatch(transform, ast, keyCode, {
});

tryCatch(findPlaces, ast, text, resultOptions);

return test.extend({
    transform: transform(dir, linterOptions, options),
});


 const places = await transformAsync(ast, source, {});