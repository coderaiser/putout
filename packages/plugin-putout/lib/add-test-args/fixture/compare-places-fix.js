test('', async ({comparePlaces}) => {
    await comparePlaces();
});

test.skip('', async ({comparePlaces}) => {
    await comparePlaces();
});

test.only('', async ({comparePlaces}) => {
    await comparePlaces();
});
