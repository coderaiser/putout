export const match = () => ({
    '__a(__args': (vars, path) => {
        
        fn(x);
        
        return __args[__a];
    }
});


export const replace = () => ({
    '__a(__args': (vars, path) => {
        const __aPath = path.get('right.body');
    }
});