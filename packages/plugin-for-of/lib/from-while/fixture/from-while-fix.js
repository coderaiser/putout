for (const p of node.signature.params) {
    params.push(identifier(p.name.value));
}

for (const p of node.signature.params) {
    params.push(identifier(p.name.value));
}

for (const p of node.signature.params) {
    params.push(identifier(p.name.value));
}
