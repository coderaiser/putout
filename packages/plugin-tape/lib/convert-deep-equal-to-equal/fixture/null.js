t.deepEqual(result, null);
t.deepEqual(result, undefined);

const expectedNull = null;

t.deepEqual(result, expectedNull);

const expectedUndefined = undefined;

t.deepEqual(result, expectedUndefined);
