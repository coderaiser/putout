{
    const expected = [{
        id: 1,
        x: 'hello',
    }];
    deepEqual(rows, expected);
}
{
    const expected = {
        id: 1,
        x: 'hello',
    };
    deepEqual(rows, expected);
}
