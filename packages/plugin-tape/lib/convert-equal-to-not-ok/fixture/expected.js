const expected = false;
t.equal(result, expected);

const expectedNull = null;
t.equal(result, expectedNull);

const expectedUndefined = undefined;
t.equal(result, expectedUndefined);

t.equal(result, undefined);
