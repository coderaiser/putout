import {test} from 'supertape';

test('primitive', (t) => {
    {
        const expected = result;
        t.equal(equal, expected);
    }
    t.end();
});

test('object', (t) => {
    {
        const expected = {
            ast: null,
        };
        t.deepEqual(result, expected);
    }
    t.end();
});
