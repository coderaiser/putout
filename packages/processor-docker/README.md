# @putout/processor-docker [![NPM version][NPMIMGURL]][NPMURL]

[NPMIMGURL]: https://img.shields.io/npm/v/@putout/processor-docker.svg?style=flat&longCache=true
[NPMURL]: https://npmjs.org/package/@putout/processor-docker "npm"

> A Dockerfile is a text document that contains all the commands a user could call on the command line to assemble an image. This page describes the commands you can use in a Dockerfile.
>
> (c) [docker.com](https://docs.docker.com/reference/dockerfile/)

🐊[**Putout**](https://github.com/coderaiser/putout) processor adds ability to parse `Dockerfile` to `json` and back.

## Install

```
npm i @putout/processor-toml -D
```

## Usage

```json
{
    "processors": ["docker"]
}
```

## License

MIT
