if (noTrailingComment(path) && (length > 1 || manyLines))
    // maybe.print(index !== n || trailingComma, ',');
    print.newline();

if (noTrailingComment(path) && (length > 1 || manyLines))
    print.newline();

// maybe.print(index !== n || trailingComma, ',');
const a = 5;

if (CloudCmd.config('menu') === 'aleman') {
    //const {createMenu} = await import(`https://esm.sh/aleman@1.1.31/menu?alias=putout:@putout/bundle`);
    return createMe;
}
