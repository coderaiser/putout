for (const o of l) {
    var c = o.sizer;

    for (const [w, v] of g.entries())
        if (v && v.length > 1)

            var t = c.appendChild(document.createElement('span'));
        else
            m[w] = y;
}
