maybe.indent.dec(!insideArray);
maybe.indent.dec(insideArray);

maybe.indent.inc(!insideArray);
maybe.indent.inc(insideArray);

maybe.indent.dec(!insideArray);
maybe.indent.inc(insideArray);

maybe.indent.dec(!insideArray);
maybe.indent.dec(!insideArray);
