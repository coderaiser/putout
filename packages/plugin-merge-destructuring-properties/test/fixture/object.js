const item = getItem();

const {username} = item;
const {password} = item;

function hello() {
    const item = {
        username: 'hello',
        password: 'world'
    };
    
    const {username} = item;
    const {password} = item;
}

