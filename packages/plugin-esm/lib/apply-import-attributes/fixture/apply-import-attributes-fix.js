import a from '../package.json' with {
    type: 'json',
};
import client from './client.js';

export {default as package} from './package.json' with {
    type: 'json',
};

export __export from 's';
export __export from './package.json' with {
    type: 'json',
};

export {
    hello,
};
