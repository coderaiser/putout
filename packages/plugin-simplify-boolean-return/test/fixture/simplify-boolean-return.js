function isA(a, b) {
    if (a.length !== b.length)
        return false;

    return true;
}

function isB(a, b) {
    if (a.length !== b.length)
        return true;

    return false;
}

function isC(a, b) {
    if (a.length !== b.length)
        return true;

    return true;
}

function isD(a, b) {
    if (a.length !== b.length)
        return false;

    return false;
}
