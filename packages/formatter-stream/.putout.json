{
    "printer": "putout",
    "formatter": "stream",
    "rules": {
        "babel/transform-inline-consecutive-adds": ["on", "Avoid consecutive adds"]
    },
    "plugins": ["babel/transform-inline-consecutive-adds"]
}
