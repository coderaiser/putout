{
    "rules": {
        "rm-unused-vars": true,
        "rm-debugger": true,
        "rm-only": true
    },
    "plugins": [
        "rm-unused-vars",
        "rm-debugger",
        "rm-only"
    ]
}

