const {isArray} = Array;

export const c = () => {
    const b = isArray(a) ? [] : {};
    const m = x[i];
    const value = object[key];
    
    if (isArray(object)) {}
    
    const isObject = value instanceof Object;
};
