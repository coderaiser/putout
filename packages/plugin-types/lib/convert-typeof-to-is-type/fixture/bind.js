const isBool = () => false;

function some() {
    const isString = a;
    
    if (a) {
        const {
            m = typeof a === 'string' ? 1 : 2
        } = x;
    }
    
    if (typeof a === 'bool')
        return;
}
