let options = {};

const {screenReaderMode} = this._terminal.options;

if (a > b) {
    const {
        buffer,
        charset,
        cols,
        wraparoundMode,
        insertMode,
    } = this._terminal;
}
