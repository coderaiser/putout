function parse(params) {
    const {
        panel
    } = params;
    
    const {
        history
    } = params;
    
    const {
        noCurrent
    } = params;
    
    const {
        currentName
    } = params;
}

