const item = getItem();

const {username, password} = item;

function hello() {
    const item = {
        username: 'hello',
        password: 'world',
    };
    
    const {username, password} = item;
}
