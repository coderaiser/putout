function hello(args) {
    const {a, b} = args;
}

function no([args]) {
    const {a, b} = args;
}

function many(args) {
    const {hello, world, and, more, words} = args;
}

function long(args) {
    const {helloWorld, WorldHello} = args;
}

const {a, b} = c;

function world(params) {
    const {a, b} = args;
}

{
    const {a, b} = c;
}
