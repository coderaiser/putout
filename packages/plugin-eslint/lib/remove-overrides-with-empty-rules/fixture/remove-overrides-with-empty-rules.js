__putout_processor_json({
 "overrides": [{
    "files": ["lib/**/*.spec.js"],
    "rules": {
        "node/no-unpublished-require": "off"
    },
 }],
});

__putout_processor_json({
 "overrides": [{
    "files": ["lib/**/*.spec.js"],
    "rules": {
    },
 }],
});

__putout_processor_json({
    "rules": {
    },
     "overrides": [{
        "files": ["lib/**/*.spec.js"],
        "rules": {
        },
     }]
});

__putout_processor_json({
    "overrides": [{
        "files": ["lib/**/*.spec.js"],
        "rules": {
        },
    }, {
        "files": ["lib/**/*.spec.js"],
        "rules": {
        },
    }]
});

__putout_processor_json({
    "other": [{
        "files": ["lib/**/*.spec.js"],
        "rules": {
        },
    }]
});
