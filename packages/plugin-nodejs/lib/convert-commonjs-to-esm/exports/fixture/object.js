const {parseRules} = require('./parse-rules');
const {mergeRules} = require('./merge-rules');

module.exports = {
    parseRules,
    mergeRules: _mergeRules,
};

module.exports = {
    OK: 0,
    PLACE: 1,
}

module.exports = {
    hello,
    world: 'world',
}
