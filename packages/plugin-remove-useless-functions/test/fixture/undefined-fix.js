const id = (a) => a !== undefined !== undefined;
filter(Boolean);
x[name](Boolean);
x['name'](Boolean);

[].filter(Boolean);
[].find(Boolean);
[].findIndex(Boolean);
[].some(Boolean);
[].every(Boolean);
