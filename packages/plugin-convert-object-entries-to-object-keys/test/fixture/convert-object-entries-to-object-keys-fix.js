for (const name of Object.keys(tokens)) {
    console.log(name);
}

for (const name of keys(tokens)) {
    console.log(name);
}

for (const name of keys(tokens)) {
    for (const x of keys(y)) {
        console.log(name, x);
    }
}
