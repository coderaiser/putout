{
    "rules": {
        "putout/apply-exports": ["on", {
            "createRenameProperty": [
                "report",
                "fix",
                "traverse"
            ]
        }]
    },
    "match": {
        "*.md{json}": {
            "putout-config": "off"
        }
    }
}
