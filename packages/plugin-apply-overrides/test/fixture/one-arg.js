const isParser = ({rule}) => rule.startsWith('parser');
function getDOM({
    link = {},
    getCurrentDirPath = stub(),
    getCurrentDirName = stub(),
    getByDataName = stub(),
    isContainClass = stub(),
    getCurrentType = stub(),
    getCurrentPath = stub(),
} = {}) {}
